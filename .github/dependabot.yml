# Dependabot version updates are intentionally disabled on this repository.
#
# Keeping this file in place on the default branch ensures that Dependabot
# does not open automated dependency-update pull requests. To re-enable
# version updates in the future, replace this file with a configuration
# generated from
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
version: 2
updates: []
